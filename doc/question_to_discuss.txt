Q1. Dimensions like ; head_dim = h_qkv = config.hidden_size // config.num_attention_heads
might lead to special consideration if not divisable.

Q2. For RoPE, where MLEN and head_dim relationship need to be considered. Will MLEN be set to be larger than head_dim?

Q3. Should we assume MLEN is shared between matrix machine and the vector machine?

Q4. Consider offset? for Q, K, V linear.

Q5. Dimensions mismatch
e.g. Fetching k_i in flashatten

Q6. How to effectively address a matrix?