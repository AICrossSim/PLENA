{
  "MatrixMachine": {
    "Coefficients": {
      "P1": 1,
      "P2": 1,
      "P3": 1,
      "P4": 1,
      "P5": 1
    },

    "Relationship": "P1 * MLEN * BLEN * (M_FP_EXP_WIDTH + M_FP_MANT_WIDTH + 1) + P2 * MLEN * BLEN * (WT_MXFP_MANT_WIDTH + WT_MXFP_EXP_WIDTH + 1 + MXFP_SCALE_WIDTH) + P3 * (MLEN // BLOCK_DIM) * BLEN * (BLOCK_DIM * (ACT_MXFP_MANT_WIDTH + ACT_MXFP_EXP_WIDTH + 1) + MXFP_SCALE_WIDTH) + P4 * MLEN * (V_FP_EXP_WIDTH + V_FP_MANT_WIDTH + 1) + P5"
  },
  "MatrixSRAM": {
    "Coefficients": {
      "P1": 1,
      "P2": 1
    },
    "Relationship": "P1 * MLEN * (WT_MXFP_EXP_WIDTH + WT_MXFP_MANT_WIDTH + 1 + MXFP_SCALE_WIDTH) + P2"
  },
  "VectorMachine": {
    "Coefficients": {
      "P1": 1,
      "P2": 1
    },
    "Relationship": "P1 * VLEN * (V_FP_EXP_WIDTH + V_FP_MANT_WIDTH + 1) + P2"
  },
  "VectorSRAM": {
    "Coefficients": {
      "P1": 1,
      "P2": 1
    },
    "Relationship": "P1 * VLEN * (V_FP_EXP_WIDTH + V_FP_MANT_WIDTH + 1) + P2"
  },
  "ScalarMachine": {
    "Coefficients": {
      "P1": 1,
      "P2": 1,
      "P3": 1,
      "P4": 1,
      "P5": 1
    },
    "Relationship": "P1 * (S_FP_EXP_WIDTH + S_FP_MANT_WIDTH + 1) + P2 * INT_DATA_WIDTH + P3 * INT_SRAM_WIDTH * INT_DATA_WIDTH + P4 * FP_SRAM_WIDTH * (S_FP_EXP_WIDTH + S_FP_MANT_WIDTH + 1) + P5"
  },
  "HBMSystem": {
    "Coefficients": {
      "P1": 1,
      "P2": 1,
      "P3": 1,
      "P4": 1,
      "P5": 1,
      "P6": 1
    },
    "Relationship": "P1 * HBM_ELE_WIDTH + P2 * HBM_SCALE_WIDTH + P3 * (MLEN // BLOCK_DIM) * (BLOCK_DIM * (WT_MXFP_MANT_WIDTH + WT_MXFP_EXP_WIDTH + 1) + MXFP_SCALE_WIDTH) + P4 * (MLEN // BLOCK_DIM) * (BLOCK_DIM * (KV_MXFP_MANT_WIDTH + KV_MXFP_EXP_WIDTH + 1) + MXFP_SCALE_WIDTH) + P5 * (MLEN // BLOCK_DIM) * BLEN * (BLOCK_DIM * (ACT_MXFP_MANT_WIDTH + ACT_MXFP_EXP_WIDTH + 1) + MXFP_SCALE_WIDTH) + P6"
  }
}
